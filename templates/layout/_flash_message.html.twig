{% for type in (['success','danger', 'info']) %}
    {% for message in app.flashes(type) %}
        <div class="col-md-12 mx-auto mt-2">
            <div class="alert alert-{{ type }} alert-dismissible fade show">
                {{ message }}
            </div>
        </div>
    {% endfor %}
{% endfor %}