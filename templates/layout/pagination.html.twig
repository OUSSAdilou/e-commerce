{% if pageCount > 1 %}
    <nav aria-label="...">
    <ul class="pagination" style="">
        {% if previous is defined %}
            <li class="page-item">
            <a class="page-link" href="{{path(route, query|merge({(pageParameterName):previous})) }}">Précédent</a>
            </li>
        {% endif %}

        {# {% for page in pagesInRange %}
            {% if page != current %}
                <li class="page-item active" aria-current="page">
                <a style="border-radius: 50%" class="page-link" href="{{path(route, query|merge({(pageParameterName):page})) }}" >{{ page }}</a>
            {% else %}
            <li class="page-item active" aria-current="page">
                <a style="border-radius: 50%; background: green" class="page-link disebled text-whie-50" href="{{path(route, query|merge({(pageParameterName):page})) }}">{{ page }}</a>
            {% endif %}
        {% endfor %} #}
        
        {# <li class="page-item"><a class="page-link" href="#">1</a></li>
        </li> #}
        {# <li class="page-item"><a class="page-link" href="#">3</a></li> #}
        {# <li class="page-item"> #}
        {% if next is defined %}
            <a class="page-link" href="{{path(route, query|merge({(pageParameterName):next}))}}">Suivant</a>
            </li>
        {% endif %}
    </ul>
    </nav>
{% endif %}