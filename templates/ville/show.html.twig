{% extends 'base.html.twig' %}

{% block title %}Ville{% endblock %}

{% block body %}
    <div class="container">
        <h1>Ville</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ville.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ville.nom }}</td>
            </tr>
            <tr>
                <th>FraisLivraison</th>
                <td>{{ ville.fraisLivraison }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-info" href="{{ path('app_ville_index') }}">Retour à la liste</a>

    <a class="btn btn-outline-warning" href="{{ path('app_ville_edit', {'id': ville.id}) }}">Modifier</a><br>

    {{ include('ville/_delete_form.html.twig') }}
    </div>
{% endblock %}
