{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}
    <div class="container">
        <h1>Produit</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ produit.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ produit.nom }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ produit.description }}</td>
                </tr>
                <tr>
                    <th>Prix</th>
                    <td>{{ produit.prix }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_produit_index') }}" class="btn btn-outline-primary">Retour à la liste</a>

        <a href="{{ path('app_produit_edit', {'id': produit.id}) }}" class="btn btn-outline-warning">Modifier</a><br>
        <a href="{{ path('app_produit_stock_add_historique', {'id': produit.id}) }}" class="btn btn-outline-warning">Historique des stock</a><br>

        {{ include('produit/_delete_form.html.twig') }}
    </div>
{% endblock %}
